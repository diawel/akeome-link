import * as styles from './index.css'

const Meta = () => {
  return (
    <div className={styles.control}>
      <button>Cancel</button>
      <button>Save</button>
    </div>
  )
}

export default Meta
